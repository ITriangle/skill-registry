---
description: 对协作者说话要直白、先讲业务含义；方案和问答都适用。
alwaysApply: true
---

# 对协作者说话

对最终用户的文案仍遵循 `AGENTS.md`。本规则管的是：**Agent 在对话、方案、问答里怎么跟协作者讲。**

- 先说「对你意味着什么」，再必要时补一句怎么做。结论在前，机制最多三步。
- 用对方已经在用的词（正式版客户端、账号目录、隔离目录、内置技能）。不要把内部模块名、环境变量、流程图当主语。
- 必须出现路径或命令时，放在那句人话后面当括号。
- 问「为什么」只答原因，不要改成让对方选题。需要拍板时先用一句话说清差别，再问一件事。
- 方案里「要拍板的产品决定」和「实现细节」分开；文件名、模块名默认不进主文。
- 解释概念用「等于」，不要发明新词。删掉内部名词后句子不成立，就先改句子。
- 英文术语不要硬译成没人说的词。wall-clock 就说「你等了多久」「实际花了多久」，不要写成「墙钟」；confirmation gate 就说「还是要你说开始才跑」，不要写成「闸门」。听不懂就用已经在说的词，不要另造一个「更专业」的译名。
- 对方已经起了名就沿用。不要另起诗意说法、产品黑话、拟人动作去包一层。界面就说界面上的字（已置顶、任务）；库里的标记就说他们报过的字段（pinned=1）。听不懂就用「等于」指到能看见的那一处，不要换一个更「好懂」的新名字。

```text
❌ 需要对齐 pinAccountHomeChrome 与 nuonuo unwrap 后的 HERMES_BUNDLED_SKILLS。
✅ 窗口里的助手和终端里的同一条命令，应看到同一份内置技能。评测换一个空目录跑，避免写进你正在用的账号。

❌ 点置顶时窗口先改名单，再把「请留下」写进账号档案。
✅ 点置顶后，侧边栏「已置顶」马上变。会话上还有一份 pinned=1，给清理过期会话、换电脑打开同一账号用。这次界面少画了一条，那份 pinned 没写错。

❌ 墙钟主要花在隔离执行里。
✅ 你等的时间主要花在隔离环境里跑评测。本轮先不改那块脚本。

❌ 确认闸门不变。
✅ 还是要你说「开始评测」才跑。
```
