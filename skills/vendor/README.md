# Vendored Skills

Vendored skills are stored under:

```text
skills/vendor/<source>/<skill>/
```

The directory name becomes the symlink name used in projects unless a future
manifest explicitly adds an alias field.

