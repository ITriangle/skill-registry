interface:
  display_name: "GitHub Code Review"
  short_description: "只读审查 GitHub PR、本地分支与工作区代码变更并输出分级发现"
  default_prompt: "Use $github-code-review to perform a read-only gated review of this PR or local diff."
